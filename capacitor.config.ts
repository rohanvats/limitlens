import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.limitlens',
  appName: 'limitlens',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
